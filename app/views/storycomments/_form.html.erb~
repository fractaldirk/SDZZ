<%= form_for([@story, @story.storycomments.build]) do |f| %>
  <div class="field">
    <%= f.label :commenter, "Waaaaat is jouw naam?" %><br />
    <%= f.text_field :commenter %>
  </div>
  <div class="field">
    <%= f.label :body, "Gevat bijschrift" %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
